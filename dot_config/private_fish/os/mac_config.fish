# Defined in - @ line 1
function android_studio 
	open -a "Android Studio" $argv;
end

function AndroidStudio
  open -a "Android Studio" $argv;
end

function subl
  open -a "Sublime Text" $argv;
end

function sublimerge
  open -a "Sublime Merge" $argv;
end

function gittyup
  open -a "Gittyup" $argv;
end

